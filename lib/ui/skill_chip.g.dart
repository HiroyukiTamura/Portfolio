// GENERATED CODE - DO NOT MODIFY BY HAND

part of 'skill_chip.dart';

// **************************************************************************
// FunctionalWidgetGenerator
// **************************************************************************

class SkillChipIcon extends StatelessWidget {
  const SkillChipIcon({
    Key? key,
    required this.label,
    required this.icon,
    required this.iconColor,
  }) : super(key: key);

  final String label;

  final IconData icon;

  final Color iconColor;

  @override
  Widget build(BuildContext _context) => _skillChipIcon(
        label: label,
        icon: icon,
        iconColor: iconColor,
      );
}

class SkillChip extends StatelessWidget {
  const SkillChip({
    Key? key,
    required this.label,
    required this.avatar,
  }) : super(key: key);

  final String label;

  final Widget avatar;

  @override
  Widget build(BuildContext _context) => _skillChip(
        label: label,
        avatar: avatar,
      );
}

class SkillChipAdmob extends StatelessWidget {
  const SkillChipAdmob({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext _context) => _skillChipAdmob();
}

class SkillChipJira extends StatelessWidget {
  const SkillChipJira({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext _context) => _skillChipJira();
}

class SkillChipTypeScript extends StatelessWidget {
  const SkillChipTypeScript({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext _context) => _skillChipTypeScript();
}

class SkillChipFirebase extends StatelessWidget {
  const SkillChipFirebase({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext _context) => _skillChipFirebase();
}

class SkillChipKotlin extends StatelessWidget {
  const SkillChipKotlin({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext _context) => _skillChipKotlin();
}

class SkillChipFlutter extends StatelessWidget {
  const SkillChipFlutter({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext _context) => _skillChipFlutter();
}

class SkillChipDart extends StatelessWidget {
  const SkillChipDart({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext _context) => _skillChipDart();
}
